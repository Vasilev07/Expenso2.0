export interface IUser {
    email: string;
    password: string;
    darkMode?: boolean;
    currency: string;
}

export interface IUserDetails {
    email: string;
    id: string;
    darkMode: boolean;
    currency: string;
}
